Electron-react app for hotel client managing

Clone with `git clone` command

`cd` to the project directory and execute `yarn` or `npm i` to install dependencies

Run `npm start` or `yarn start` to run project